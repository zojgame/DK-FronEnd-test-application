import {Layout} from 'antd';
const {Header} = Layout;

function HeaderComponent():JSX.Element {
    return (
        <Layout>
            <Header>
            </Header>
        </Layout>);
}

export default HeaderComponent;